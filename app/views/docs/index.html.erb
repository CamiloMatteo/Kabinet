<% @docs.each do |doc|%>
  <h2><%= link_to doc.title, doc %></a></h2>
  <p><%= time_ago_in_words(doc.created_at) %></p>
  <p><%= truncate(doc.content, length: 10) %></p>
<% end %>

<%= link_to "New Doc", new_doc_path %>